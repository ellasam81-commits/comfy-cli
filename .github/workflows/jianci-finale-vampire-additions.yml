name: Jian Ci Finale Vampire Additions
on:
  push:
    branches: [codex/jianci-transition-20260905]
    paths: [video-tests/jianci-finale-vampire-additions/run.request.json]
permissions:
  contents: read
  actions: read
concurrency:
  group: jianci-finale-vampire-additions
  cancel-in-progress: false
jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install ffmpeg
        run: sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg
      - name: Generate six bounded vampire inserts
        env:
          XRTOKEN: ${{ secrets.XRTOKEN }}
          JIANCI_EXECUTE: '1'
        run: python video-tests/jianci-finale-vampire-additions/generate.py
      - name: Upload targeted repair result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jianci-finale-vampire-additions
          path: output/jianci-finale-vampire-additions
          retention-days: 14
