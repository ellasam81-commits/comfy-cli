name: Jian Ci Episode 03 Dialogue Repair
on:
  push:
    branches: [codex/jianci-transition-20260905]
    paths: [video-tests/jianci-ep03-repair/run.request.json]
permissions:
  contents: read
concurrency:
  group: jianci-ep03-repair
  cancel-in-progress: false
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install media tools
        run: sudo apt-get update -qq && sudo apt-get install -y ffmpeg
      - name: Generate two episode3 dialogue repairs
        env:
          SEGMIND_API_KEY: ${{ secrets.SEGMIND_API_KEY }}
          JIANCI_EP03_EXECUTE: '1'
        run: python video-tests/jianci-ep03-repair/generate.py
      - name: Save raw before speech check
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jianci-ep03-repair-raw
          path: output/jianci-ep03-repair
          retention-days: 7
      - name: Install speech checker
        if: success()
        run: pip install faster-whisper
      - name: Transcribe returned dialogue
        if: success()
        run: python video-tests/jianci-ep03-repair/transcribe.py
      - name: Save transcript
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jianci-ep03-repair-transcript
          path: output/jianci-ep03-repair/transcription.json
          retention-days: 7

